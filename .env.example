# 选择图像后端：openai / glibatree
IMAGE_BACKEND=openai

# OpenAI 设置（后端选择 openai 时必填）
OPENAI_API_KEY=sk-xxxx
# 可选项：自定义 API Base（如走代理网关时）/ 模型
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-image-1

# --- Google Cloud / Vertex 基础 ---
GCP_PROJECT_ID=
GCP_LOCATION=us-central1
# 鉴权二选一：
# 1) Render 场景推荐直接粘贴 JSON 字符串
GOOGLE_APPLICATION_CREDENTIALS_JSON=
# 或者（非必要，不推荐）：
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json

# --- Imagen3 模型配置 ---
VERTEX_IMAGEN_MODEL_GENERATE=imagen-3.0-generate
VERTEX_IMAGEN_MODEL_EDIT=imagen-3.0-edit
VERTEX_TIMEOUT_SECONDS=60
VERTEX_SAFETY_FILTER_LEVEL=block_some  # block_few|block_some|block_most
VERTEX_SEED=0                          # 0/空 = 随机；非0=可复现

# CORS
ALLOWED_ORIGINS=https://zhaojfifa.github.io/ai-service, http://localhost:5173, http://localhost:3000

# 邮件发送（你之前已配好继续保留）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=xxx@gmail.com
SMTP_PASS=应用专用16位密码
FROM_EMAIL=xxx@gmail.com

# （可选）保留 Glibatree，作为回退
GLIB_URL=https://designer.glibatree.com/api/v1/design
GLIB_KEY=glb_live_xxx
